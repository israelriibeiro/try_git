name: CI - Build

on:
  push:
    branches: [master]


jobs:
  build:
    name: 'build and push'
    environment: production
    runs-on: ubuntu-latest

    steps:
      - uses:  actions/checkout@v4

      - name: Print environment 
        id: print_environment
        env:
          RAIL_DB: ${{ vars.RAILS_DB }}  
        run: |
          echo "Print variable $RAIL_DB"
